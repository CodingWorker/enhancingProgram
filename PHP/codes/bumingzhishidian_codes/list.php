<?php
$arr=[1,2,3,'a'=>12,'cc'=>123];
list($a,$b)=each($arr);
var_dump($a);
var_dump($b);
var_dump(each($arr));
var_dump(each($arr));
var_dump(each($arr));
var_dump(each($arr));
var_dump(each($arr));
var_dump(each($arr));
var_dump(each($arr));
var_dump(each($arr));


